<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppPersons.Views.NewPersonPage"
             Title="New Person">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Tc No" FontSize="Medium" />
            <Entry Text="{Binding Kisi.TcNo}" d:Text="" FontSize="Small" />
            <Label Text="Ad Soyad" FontSize="Medium" />
            <Editor Text="{Binding Kisi.AdSoyad}" d:Text="" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>


</ContentPage>